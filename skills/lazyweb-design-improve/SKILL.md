---
name: lazyweb-design-improve
router-exclude: true
description: |
  DEPRECATED — moved. This skill has been folded into the lazyweb-design
  umbrella under `objective=improve`. Do not run it; route to the new skill.
---

# Deprecated — moved

This skill has moved. Improving or critiquing an existing screen is now handled
by **`/lazyweb-design`** with `objective=improve`.

1. Run `/lazyweb-update` to refresh your local Lazyweb skill pack.
2. Use `/lazyweb-design` (objective=improve) instead of this skill.
